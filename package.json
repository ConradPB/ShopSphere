{
  "name": "shopsphere",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client:dev": "cd client && yarn dev",
    "server:dev": "cd server && pnpm start",
    "test": "pnpm run test --filter=server && yarn test --prefix client",
    "install-all": "pnpm install --filter=server && yarn install --cwd client"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  }
}